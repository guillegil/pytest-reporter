# pytest-reporter

A pytest plugin for generating custom JSON test reports.

## Installation

```bash
pip install pytest-reporter
```

## Usage

```bash
pytest --report=report.json
```

This generates a JSON file with test results including pass/fail/skip counts and per-test details.
